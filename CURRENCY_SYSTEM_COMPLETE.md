# 汇率系统完整修复

## 完成的修复

### 1. 用户默认币种支持 ✅
- 从用户信息中读取默认币种（default_currency）
- 所有金额显示使用用户的默认币种
- 支持多种币种符号显示

### 2. 汇率管理器改造 ✅
- 显示用户的默认币种提示
- 过滤掉默认币种（默认币种汇率始终为 1）
- 所有汇率都是相对于用户默认币种的汇率
- 汇率设置时明确显示"1 XXX = ? 默认币种"

### 3. 账本详情页改造 ✅
- 总支出显示用户默认币种
- 预算进度显示用户默认币种
- 个人/共同支出统计显示用户默认币种
- 日均消费显示用户默认币种
- 类别统计显示用户默认币种
- 成员统计显示用户默认币种
- 按天查看显示用户默认币种

### 4. 交易显示优化 ✅
- 主金额显示为本币金额（local_amount）
- 如果原币种不是默认币种，显示原币金额作为辅助信息
- 格式：主金额（默认币种） + 原币金额（原币种）

## 支持的币种

| 币种代码 | 币种名称 | 符号 |
|---------|---------|------|
| CNY | 人民币 | ¥ |
| USD | 美元 | $ |
| EUR | 欧元 | € |
| JPY | 日元 | ¥ |
| GBP | 英镑 | £ |
| THB | 泰铢 | ฿ |
| KRW | 韩元 | ₩ |

## 数据流程

### 场景 1：用户默认币种是 CNY

1. **设置汇率**
   - 用户设置：1 THB = 0.2 CNY
   - 系统保存到 exchange_rates 表

2. **创建交易**
   - 用户输入：1000 THB
   - 系统计算：local_amount = 1000 * 0.2 = 200 CNY
   - 保存：amount=1000, currency=THB, local_amount=200

3. **显示**
   - 主金额：¥200
   - 原币金额：฿1000

4. **总支出**
   - 所有交易的 local_amount 相加
   - 显示：¥200

### 场景 2：用户默认币种是 USD

1. **设置汇率**
   - 用户设置：1 CNY = 0.14 USD
   - 用户设置：1 THB = 0.028 USD

2. **创建交易**
   - 交易 1：100 CNY → local_amount = 100 * 0.14 = 14 USD
   - 交易 2：1000 THB → local_amount = 1000 * 0.028 = 28 USD

3. **显示**
   - 交易 1：$14 (¥100)
   - 交易 2：$28 (฿1000)

4. **总支出**
   - 14 + 28 = $42

## 汇率更新机制

### 更新汇率时的影响
1. 用户修改汇率（如 1 THB = 0.2 CNY 改为 0.21 CNY）
2. 系统更新 exchange_rates 表
3. 系统重新计算所有该币种的交易 local_amount
4. 总支出自动更新

### SQL 更新语句
```sql
UPDATE transactions 
SET local_amount = amount * 0.21
WHERE account_book_id = ? AND currency = 'THB'
```

## 用户设置默认币种

### 前端
用户可以在个人设置页面修改默认币种

### 后端
```javascript
// 更新用户默认币种
await db.query(
  'UPDATE users SET default_currency = ? WHERE id = ?',
  [newCurrency, userId]
);
```

### 注意事项
1. 修改默认币种不会影响已有交易的 local_amount
2. 建议在账本创建前设置好默认币种
3. 如果要改变默认币种，需要重新设置所有汇率

## 显示规则

### 金额显示优先级
1. **主金额**：始终显示 local_amount（用户默认币种）
2. **辅助金额**：如果原币种 ≠ 默认币种，显示原币金额

### 示例
- 默认币种 CNY，交易 100 CNY：只显示 ¥100
- 默认币种 CNY，交易 1000 THB：显示 ¥200 (฿1000)
- 默认币种 USD，交易 100 CNY：显示 $14 (¥100)

## 测试场景

### 测试 1：设置汇率
1. 登录系统
2. 进入账本详情
3. 点击汇率管理
4. 查看默认币种提示
5. 选择一个币种（非默认币种）
6. 输入汇率
7. 保存

### 测试 2：创建交易
1. 点击记账
2. 选择币种和金额
3. 查看汇率提示（应显示换算后的本币金额）
4. 保存交易
5. 查看账本详情，确认金额正确

### 测试 3：更新汇率
1. 创建几笔 THB 交易
2. 记录总支出
3. 修改 THB 汇率
4. 刷新页面
5. 确认总支出已更新

### 测试 4：多币种混合
1. 创建 CNY 交易
2. 创建 THB 交易
3. 创建 USD 交易
4. 查看总支出（应该是所有 local_amount 的和）
5. 查看各个交易的显示格式

## 修改的文件

1. `frontend/src/components/ExchangeRateManager.jsx`
   - 添加用户默认币种获取
   - 添加币种符号映射
   - 过滤默认币种
   - 更新所有显示文本

2. `frontend/src/pages/AccountBookDetail.jsx`
   - 添加用户默认币种获取
   - 添加币种符号映射
   - 更新所有金额显示
   - 优化交易显示格式

3. `frontend/src/components/ExchangeRateManager.css`
   - 添加默认币种提示样式

## 后续优化建议

1. **汇率历史记录**
   - 记录每次汇率修改的历史
   - 可以查看汇率变化趋势

2. **自动获取汇率**
   - 集成第三方汇率 API
   - 自动更新实时汇率

3. **汇率提醒**
   - 汇率波动提醒
   - 建议最佳换汇时机

4. **多账本汇率共享**
   - 同一用户的多个账本可以共享汇率设置
   - 减少重复设置

5. **汇率计算器**
   - 提供独立的汇率计算工具
   - 方便用户快速换算
