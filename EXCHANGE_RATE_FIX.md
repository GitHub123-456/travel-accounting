# 汇率计算修复方案

## 问题分析

### 当前问题
1. 汇率管理器硬编码了 CNY 作为本币
2. 没有使用用户设置的默认币种
3. 总支出计算时没有统一换算到用户的默认币种

### 需求
1. 用户可以设置自己的默认币种（如 CNY、USD、EUR 等）
2. 所有汇率都是相对于用户默认币种的汇率
3. 总支出自动换算成用户的默认币种显示
4. 用户可以手动设置和更新汇率

## 解决方案

### 1. 获取用户默认币种
在前端需要获取并存储用户的默认币种信息

### 2. 汇率管理器改造
- 显示用户的默认币种而不是硬编码 CNY
- 汇率设置时使用用户的默认币种作为基准

### 3. 后端计算逻辑
- 创建交易时，根据汇率表计算 local_amount
- local_amount 始终是用户默认币种的金额
- 如果交易币种就是默认币种，local_amount = amount

### 4. 总支出计算
- 所有交易的 local_amount 相加即为总支出
- 显示时带上用户的默认币种符号

## 实现步骤

### 步骤 1：前端获取用户信息
在登录后获取用户的默认币种，存储在全局状态或 localStorage

### 步骤 2：修改汇率管理器
- 从用户信息中读取默认币种
- 替换所有硬编码的 CNY

### 步骤 3：修改交易表单
- 显示汇率时使用用户默认币种
- 计算预览金额时使用正确的汇率

### 步骤 4：修改账本详情
- 总支出显示用户默认币种
- 所有金额统计使用 local_amount

### 步骤 5：后端验证
- 确保 local_amount 计算正确
- 汇率更新时重新计算所有相关交易

## 数据流程

### 创建交易
1. 用户选择币种（如 THB）和金额（如 1000）
2. 系统查询 THB 对用户默认币种的汇率（如 1 THB = 0.2 CNY）
3. 计算 local_amount = 1000 * 0.2 = 200 CNY
4. 保存交易记录

### 更新汇率
1. 用户修改 THB 的汇率为 0.21
2. 系统更新汇率表
3. 系统重新计算所有 THB 交易的 local_amount
4. 总支出自动更新

### 显示总支出
1. 查询所有交易的 local_amount
2. 求和得到总支出
3. 显示为：¥200（如果默认币种是 CNY）
4. 显示为：$30（如果默认币种是 USD）

## 币种符号映射

```javascript
const CURRENCY_SYMBOLS = {
  'CNY': '¥',
  'USD': '$',
  'EUR': '€',
  'JPY': '¥',
  'GBP': '£',
  'THB': '฿',
  'KRW': '₩'
};
```

## 注意事项

1. 默认币种的汇率始终为 1
2. 用户不能设置默认币种对自己的汇率
3. 汇率更新会影响历史数据的本币金额
4. 建议在汇率更新时给用户提示
