# 成员管理功能完成

## 功能概述

成员管理用于管理账本的参与人，这些成员可以在记账时选择作为付款人或参与分账的人员。

## 核心功能

### 1. 自动添加当前用户 ✅
- 首次打开成员管理时，如果没有成员，自动将当前用户添加为成员
- 成员名称使用用户昵称
- 备注标记为"本人"

### 2. 添加成员 ✅
- 输入成员姓名（必填，最多20字符）
- 输入备注信息（可选，最多50字符）
- 点击"添加"按钮保存

### 3. 编辑成员 ✅
- 左滑成员项，点击"编辑"
- 修改姓名或备注
- 点击"保存"更新

### 4. 删除成员 ✅
- 左滑成员项，点击"删除"
- 弹出确认对话框
- 确认后删除成员
- 删除成员不会影响已有账单

### 5. 与记账表单联动 ✅
- 成员列表实时同步
- 记账时可以选择成员作为付款人
- 记账时可以选择成员作为参与人
- 在记账表单中也可以快速添加成员

## 使用流程

### 首次使用
1. 进入账本详情页
2. 点击"成员管理"
3. 系统自动添加"我"作为第一个成员
4. 点击"添加成员"添加其他人

### 添加成员
1. 点击"添加成员"按钮
2. 输入姓名（如：张三）
3. 输入备注（如：朋友、同事）
4. 点击"添加"

### 编辑成员
1. 在成员列表中左滑要编辑的成员
2. 点击"编辑"
3. 修改信息
4. 点击"保存"

### 删除成员
1. 在成员列表中左滑要删除的成员
2. 点击"删除"
3. 确认删除

### 记账时使用
1. 点击记账按钮
2. 选择"共同账单"
3. 从成员列表中选择付款人
4. 从成员列表中选择参与人
5. 如果成员不够，可以点击"+ 添加成员"快速添加

## 界面说明

```
┌─────────────────────────────────┐
│ 成员管理                    ✕   │
├─────────────────────────────────┤
│                                 │
│ 👥 账本成员          共 3 人    │
│                                 │
│ ┌─────────────────────────┐    │
│ │ 👤 我                    │    │
│ │    本人                  │    │
│ └─────────────────────────┘    │
│                                 │
│ ┌─────────────────────────┐    │
│ │ 👤 张三                  │    │
│ │    朋友                  │    │
│ └─────────────────────────┘    │
│                                 │
│ ┌─────────────────────────┐    │
│ │ 👤 李四                  │    │
│ │    同事                  │    │
│ └─────────────────────────┘    │
│                                 │
│ ┌─────────────────────────┐    │
│ │ ➕ 添加成员               │    │
│ └─────────────────────────┘    │
│                                 │
│ ┌─────────────────────────┐    │
│ │ 💡 使用提示               │    │
│ │ • 成员用于记录共同账单的  │    │
│ │   参与人                 │    │
│ │ • 记账时可以选择付款人和  │    │
│ │   参与人                 │    │
│ │ • 左滑成员可以编辑或删除  │    │
│ │ • 删除成员不会影响已有账单│    │
│ └─────────────────────────┘    │
└─────────────────────────────────┘
```

## 数据结构

### participants 表
```sql
CREATE TABLE participants (
  id INT PRIMARY KEY AUTO_INCREMENT,
  account_book_id INT NOT NULL,
  name VARCHAR(50) NOT NULL,
  remark VARCHAR(100),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 字段说明
- `id`: 成员 ID
- `account_book_id`: 所属账本 ID
- `name`: 成员姓名
- `remark`: 备注信息
- `created_at`: 创建时间
- `updated_at`: 更新时间

## API 接口

### 获取成员列表
```javascript
GET /account-books/:bookId/participants
```

### 添加成员
```javascript
POST /account-books/:bookId/participants
Body: { name, remark }
```

### 更新成员
```javascript
PUT /participants/:id
Body: { name, remark }
```

### 删除成员
```javascript
DELETE /participants/:id
```

## 与记账表单的联动

### 数据流向
1. 成员管理器添加/编辑/删除成员
2. 调用 `onUpdate` 回调通知父组件
3. 父组件重新加载数据
4. 记账表单获取最新成员列表

### 实现方式
```javascript
// 成员管理器
<ParticipantManager
  visible={showParticipantManager}
  onClose={() => setShowParticipantManager(false)}
  bookId={id}
  isArchived={book?.is_archived}
  onUpdate={loadData}  // 更新回调
/>

// 记账表单
<TransactionForm
  bookId={id}
  // 表单内部会自动加载成员列表
/>
```

## 特殊处理

### 1. 自动添加当前用户
```javascript
const loadParticipants = async () => {
  const data = await participantAPI.getList(bookId);
  setParticipants(data || []);
  
  // 如果没有成员，自动添加当前用户
  if ((!data || data.length === 0) && currentUser) {
    await addCurrentUserAsParticipant();
  }
};
```

### 2. 封存账本限制
- 已封存的账本不能添加成员
- 已封存的账本不能编辑成员
- 已封存的账本不能删除成员
- 操作时会提示"已封存的账本不可修改成员"

### 3. 删除成员的影响
- 删除成员不会删除相关的交易记录
- 已有交易中的付款人和参与人信息保持不变
- 只是在新建交易时无法选择已删除的成员

## 用户体验优化

### 1. 头像显示
- 使用成员姓名的第一个字符作为头像
- 紫色渐变背景
- 圆形设计

### 2. 左滑操作
- 左滑显示编辑和删除按钮
- 编辑按钮：蓝色
- 删除按钮：红色

### 3. 空状态提示
- 无成员时显示友好提示
- 引导用户添加成员

### 4. 使用提示
- 底部显示使用提示
- 蓝色背景，醒目但不突兀
- 说明成员的用途和操作方法

## 注意事项

1. **成员数量**
   - 建议不超过 10 人
   - 太多成员会影响选择体验

2. **姓名唯一性**
   - 系统不强制姓名唯一
   - 建议使用备注区分同名成员

3. **删除限制**
   - 删除成员前会弹出确认对话框
   - 删除后无法恢复
   - 但不影响已有账单

4. **封存账本**
   - 封存后不能修改成员
   - 需要先解除封存

## 测试场景

### 测试 1：首次使用
1. 创建新账本
2. 进入成员管理
3. 确认自动添加了当前用户

### 测试 2：添加成员
1. 点击"添加成员"
2. 输入姓名和备注
3. 点击"添加"
4. 确认成员出现在列表中

### 测试 3：编辑成员
1. 左滑成员
2. 点击"编辑"
3. 修改信息
4. 点击"保存"
5. 确认信息已更新

### 测试 4：删除成员
1. 左滑成员
2. 点击"删除"
3. 确认删除
4. 确认成员已从列表中移除

### 测试 5：记账联动
1. 添加几个成员
2. 进入记账页面
3. 选择"共同账单"
4. 确认可以看到所有成员
5. 选择付款人和参与人
6. 保存账单

### 测试 6：在记账中添加成员
1. 进入记账页面
2. 选择"共同账单"
3. 点击"+ 添加成员"
4. 添加新成员
5. 确认新成员出现在选择列表中

## 文件清单

### 新增文件
1. `frontend/src/components/ParticipantManager.jsx` - 成员管理组件
2. `frontend/src/components/ParticipantManager.css` - 成员管理样式

### 修改文件
1. `frontend/src/pages/AccountBookDetail.jsx` - 集成成员管理器
2. `frontend/src/pages/TransactionForm.jsx` - 已支持成员选择

## 后续优化建议

1. **成员分组**
   - 支持将成员分组（家人、朋友、同事等）
   - 方便快速选择

2. **成员头像**
   - 支持上传自定义头像
   - 更个性化

3. **成员权限**
   - 设置成员权限（只读、可编辑等）
   - 多人协作时更安全

4. **成员统计**
   - 显示每个成员的消费统计
   - 已付金额、应付金额等

5. **快速邀请**
   - 通过微信等方式邀请成员
   - 自动加入账本
